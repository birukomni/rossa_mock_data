# Railway sets PORT automatically — do NOT set manually on Railway
PORT=4000

# Optional: artificial latency to simulate real API (milliseconds, 0 = disabled)
MOCK_DELAY_MS=0

# Comma-separated allowed CORS origins (* = allow all, fine for local dev)
CORS_ORIGINS=*
